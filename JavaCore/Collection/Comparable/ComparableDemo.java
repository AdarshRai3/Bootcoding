import java.util.*;
public class ComparableDemo {
    List<Students> ls = new ArrayList<>();
      // Adding students to the list
     ls.add(new Students("Ad",9));
}
//For a custom sorting logic we can use comparators and when we can to create a sorting logic for whole class itself then we comparables 